# Issue #8: user explicitly authorized full access for scheduled execution, 2026-09-13.
# Project-scoped defaults also apply to new interactive sessions in this project.
sandbox_mode = "danger-full-access"
approval_policy = "never"
approvals_reviewer = "user"

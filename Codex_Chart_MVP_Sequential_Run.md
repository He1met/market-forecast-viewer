# Chart MVP：Codex 串行实施授权与执行指令

适用仓库：`He1met/market-forecast-viewer`。

本文件是交给 Codex 的执行指令草案，不代表仓库已被修改或阶段已获验收。将下面的指令作为用户消息发送给 Codex，才表示批准其中列明的实施范围与授权变更。原始 `Codex_Chart_MVP_Development_Pack.md` 保持不变。

---

请在现有本地项目 He1met/market-forecast-viewer 中，按照下面的授权和规则，串行实施 Chart MVP。不要只输出计划，也不要重建仓库、重复创建 C0–C5 Issues 或扩充平台。

## 一、本次授权与最终停止点

我确认现有 C0 的 CHART_MVP、DATA_CONTRACT 作为本轮实施基线，包括它们明确标为演示的默认数据与窗口；这不代表正式预测周期、概率或交易参数已验证。

我现在授权你按依赖顺序推进：
- #1 C0：环境与文档约定确认；
- #2 C1：真实历史快照、固定 DEMO 与数据校验；
- #3 C2：真实 K 线页面与完整未来时间坐标；
- #4 C3：DEMO 多路径、分阶段区间与视觉验收材料；
- #5 C4：独立网格切换、窗口与重载错误处理；
- #6 C5：独立只读审查、范围内修复、验证与本地交付。

本授权替代此前“每阶段等待我重新授权”的实施方式：前一阶段的技术检查通过后，自动继续下一阶段，不为普通实现细节反复请求确认。

明确变更：C3 仍必须完成真实浏览器截图、坐标与交互检查，但我的第一轮视觉确认延后，与 C5 最终验收一起进行。C3 技术通过可以进入 C4；不得因此声称我已认可页面，不得把候选截图标成用户批准的回归基线。

最终停止在“Chart MVP 工程交付，待用户视觉验收”。不自动合并，不自动关闭需要用户验收的任务，不进入真实预测、自动更新、收益计算或交易。不为持续推进而创建定时任务或无限重试循环。

本授权不能绕过工具权限、沙箱、安全规则或来源使用限制；遇到这类边界应如实报告。

## 二、恢复现有上下文并消除旧授权冲突

先读取：
AGENTS.md、README.md、docs/CHART_MVP.md、docs/DATA_CONTRACT.md、docs/PROGRESS.md、原始开发包，以及 #1–#6 当前正文和相关最新评论。

检查当前目录、Git remote、分支、HEAD、未提交修改与 origin/main。此前核对点为 f4b4bf7，但以实际 Git 状态为准，禁止为匹配该哈希执行 reset 或覆盖新增工作。已有本轮分支则恢复，不另建重复分支。

在功能分支中，把本次实施授权写入现有 AGENTS.md、CHART_MVP 和 PROGRESS 的当前状态；同步里程碑与相关 Issue 的授权说明，保留旧记录为历史，不改原始开发包、不删除验收要求。不增加第二套产品规格或大量空文档。

CHART_MVP 管功能与体验，DATA_CONTRACT 管字段，PROGRESS 管已发生事实；Issue 引用它们，不再复制字段定义。本次消息仅修改上述授权与暂停安排，其余约定继续有效。修改后显式重新阅读当前 AGENTS.md，不假定当前会话已经自动刷新规则。

若发现实质矛盾，说明具体冲突和最小解决建议；不要偷偷修改数据口径、删除功能或降低验收标准。命名、局部代码组织、布局细节等不改变产品含义的问题，可自行选择最小实现并记录。

## 三、允许的实现与不可扩大范围

采用现有方案：Vite + TypeScript + 原生 HTML/CSS + 官方 lightweight-charts；一个页面、三份本地 JSON、一个统一数据加载入口。允许安装本项目必需依赖与匹配的测试浏览器、执行一次性官方公开行情下载、测试、构建和本机启动。

依赖选择兼容的正式版本，固定精确版本并提交锁文件。核对实际安装的版本和类型定义，再使用对应官方文档，不把 C0 查询版本当成已经安装，不复制过时 API。

不引入其他大模型、付费大模型 API、Kronos、数据库、Docker、微服务、登录、复杂状态管理、插件平台或多 Agent 调度平台。不实现预测器、撮合器、回测收益、行情自动更新或交易功能。

dev/preview 只监听 127.0.0.1；端口冲突报告，不杀其他项目进程。资源本地打包，页面不依赖外部 CDN，不读取交易密钥或导出认证凭证。使用已有授权工具访问 GitHub，但不得输出令牌。保留官方许可证和署名。

不修改全局代理、TLS 校验、安全设置，不安装全局工具，不改变仓库可见性，不删除用户文件或修改其他项目。

## 四、实施顺序与技术关卡

复用已经完成的 C0，不重新进行一轮泛化规划。简要说明执行顺序后开始实施。

每个阶段执行：读对应 Issue → 实现最小功能 → 运行本阶段测试 → 修复范围内问题 → 保存证据 → 更新 PROGRESS 与 Issue → 提交阶段变更 → 进入下一阶段。

“进入下一阶段”依据真实技术检查，不以 Issue 是否关闭作为依据。未满足技术前置条件，不得把阶段标成通过。

C1：一次性取得契约指定的真实 OKX BTC-USDT-SWAP 15m 历史，冻结连续 14 天完整窗口；保留来源收据。生成与其关联且可复现的 forecast.demo.json、grids.demo.json，完成文件和跨文件校验。下载失败、地区限制、缺口或字段问题必须报告；不切换来源、不编造历史、不覆盖上一份有效快照。单元测试合成样例必须明确是测试数据，不能冒充 C1 的真实数据交付。

C2：显示真实历史 K 线，建立包含锚点与全部未来节点的时间坐标；先验证真实页面和匹配浏览器环境，再推进复杂图层。

C3：显示至少三条固定 DEMO 路径、内外示意区间、阶段与历史/未来分界。真实浏览器验证缩放、平移、价格轴缩放、纯未来视窗、全部路径隐藏和 DPR；保存待用户验收截图。技术通过继续 C4，不替用户批准视觉效果。

C4：实现文件驱动的任意模式/数量方案切换、网格层级、参数摘要、6/12/24h 视窗、重置、手动重载、错误恢复。反复切换至少20次，确认无旧线残留、重复图表或监听累积。

C5：执行独立只读审查、修复已确认的问题，重跑相关与最终验证，完成干净依赖安装、启动/停止及交付说明。

## 五、图表与数据不可退让的要求

严格遵守现有 DATA_CONTRACT，不再另定字段。三文件的 ID/hash、品种、价格口径、UTC 秒、收盘边界锚点必须一致；未来96个节点不计入真实历史。

固定 DEMO 可复现；刷新、切换路径/窗口/网格不重新生成或写回数据。未来路径没有真实概率；不从三条演示推导各占1/3，也不显示虚构收益、胜率或推荐等级。

真实 K 线、路径、区间、网格共用价格坐标。使用官方公开接口与 primitive；不使用固定像素遮罩、私有图表对象或分离价格轴制造视觉对齐。坐标支撑不进入用户图例/悬停，不冒充预测。

未来全部路径隐藏或历史移出视窗后，区间、网格与时间轴仍正常。网格层级和模式从文件读取；路径开关与网格选择相互独立，不写死中性策略或三种模式。

主图必须是页面核心，不做以卡片为主的仪表盘。中文、图例、时间、价格、真实/演示边界清晰；常驻“DEMO 演示未来｜非交易信号”“示意区间，未经校准”。

错误、过期关联和重载失败必须明确展示；不把旧未来留在页面上冒充刚加载成功的新结果。按契约处理受影响图层。

## 六、测试、证据和独立审查

测试覆盖实际约定，不只检测按钮或文字存在。不得通过删除断言、跳过测试、随意放宽容差或接受错误截图来获得通过；测试自身有错时应解释修正并保留有效回归检查。

最终实测数据校验、typecheck、单元测试、test:e2e、build、npm ci 后的本机启动/停止与 preview。命令名以实际 package.json 为准，README 只写已验证可用命令。

仓库已记录完整 Chrome 无头探测超时，但这不等于浏览器测试必定失败；安装后验证与当前 Playwright 匹配的浏览器。不能用早前 Headless Shell 的基础 Canvas 成功代替本项目 E2E。

使用固定快照做测试，不在每次测试时下载新行情。按现有规格核验两个视口、DPR1/2、缩放与未来坐标、20次切换、坏文件/错误关联和阻断外网后的本地加载。

截图和测试记录写明被验证的代码版本、dataset_id、浏览器、视口、DPR、时区、命令、exit/result 和实际位置。截图只是候选验收材料，不是用户批准。不能运行的项目标记“未验证”，不能借测试数量或截图数量冒充正确性。

C5 优先使用当前工具可用的独立 Codex 只读审查能力，针对明确 base/head 与完整交付范围检查；审查者不修改工作区，实施会话是唯一写入者。记录审查对象版本、问题与复现，修复后复验相关变更。

若无法调用独立审查，就完成其余可执行验证、留下明确审查交接并标记“待独立审查”，不得把自己换个角色写的自检当成独立审查。任何审查都不能替代我的最终视觉验收。

## 七、GitHub、提交与中断恢复

本次授权创建/复用 feat/chart-mvp，按阶段提交，推送该功能分支，创建/更新一个关联 #1–#6 的 Draft PR，更新现有 Issues 和里程碑中的授权、技术状态与证据。不向 main 直接提交，不强推，不自动合并，不创建重复任务或自动化。

每项任务只勾真实完成的检查；用户验收项保持待验收。不要为显示完成而关闭 #2–#6；PR 使用 Refs #1 … #6，不用自动关闭语句。C0 可据本次明确确认更新其确认状态，但工程和视觉验收分别记录。

按 .gitignore 保留大体积与敏感产物在本地，不默认推送原始行情、node_modules、构建或完整日志。证据链接必须真实可用；本地路径就标注本地，不能伪装为 GitHub 已上传附件。GitHub暂时不可写时可继续有授权且不受影响的本地工作，记录待同步项，不虚报同步完成。

每阶段结束及预计切换上下文前更新 PROGRESS，至少包含：当前任务、实现与技术验证状态、用户验收状态、已验证代码版本、实际命令/结果、数据快照ID、证据位置、未解决项、工作区/推送状态、下一条可执行动作。

已有成果先核对再复用。重新进入会话时重读规则、PROGRESS、Git diff 与 Issue，继续第一个未完成且前置满足的工作；不重新建项目、反复下载或覆盖既有快照。

遇到真正阻塞时暂停受影响步骤，报告最小问题和所需决定；非阻塞改进记录到既有任务，不扩大本轮范围。同一失败没有新证据时，不进行无界重试。

## 八、最后交付什么

完成可执行范围后输出：
1. 我如何在本机启动、打开和停止页面，以及实际启动验证结果；
2. #1–#6 的实现、技术验证、独立审查和用户验收状态；
3. 真实来源与历史截止、固定 DEMO 和当前数据快照；
4. 实际执行的测试及结果，不可验证项；
5. 默认图、路径/区间、不同网格、缩放与错误状态的截图位置；
6. 最终功能分支、代码提交、Draft PR 和未提交/未推送事项；
7. 我需要检查的五项交互与已知局限。

最终只报告“工程已交付，待用户视觉验收”，或明确列出仍阻塞的部分。不得声称预测准确、可盈利、已完成交易系统。

现在先核对状态，把本次授权落到已有文件和任务，再开始第一个未完成阶段；不要只给我另一份计划后结束。

# Codex 开发任务书：Lightweight Charts 本地图表 MVP

版本：0.1｜日期：2026-09-12
状态：开发建议与执行提示词，尚未开发、安装或实机验证。
用途：交给官方 Codex 在用户自己的 Mac 项目目录内实施。此文档不是需要一次执行到底的长任务；每次只执行当前阶段。

## 1. 这轮只交付什么

一个可在本地浏览器打开、具有真实交互的图表页面：

- 使用官方 JavaScript/TypeScript `lightweight-charts`，不是 Python 包装器，也不是 TradingView 官网插件。
- 读取一份可追溯的真实 BTC 历史行情文件。
- 读取一份明确标注为演示的未来路径文件，展示至少三条不同形态的路径。
- 展示分时示意区间、历史/未来分界、路径图例与开关。
- 读取一份网格演示配置文件，支持切换网格方案及同步绘制网格线。
- 支持缩放、平移、十字线、重置视图和演示窗口切换。
- 数据错误能解释；测试、截图、启动说明齐全。

用户已经选择图表底座与本地显示方式。本阶段不再比较其他图表库，不引入其他预测大模型。Codex 本轮是开发者和演示数据制作者，不运行真实预测任务。

### 不在本轮范围内

真实预测准确率研究、Kronos/Chronos 等预测模型、大模型 API、定时任务、自动下载循环、WebSocket、回测引擎、真实盈亏计算、自动推荐、自动交易、浏览器扩展、账号系统、数据库、Docker、微服务、多 Agent 调度平台。

“自动更新”是图表验收之后的另一个任务。本轮只允许明确触发的一次性数据下载和手动重新加载文件。

## 2. 推荐的可修改演示默认值

这些数值只是图表验收配置，不是交易参数或已验证预测周期。

| 项目 | 默认建议 |
|---|---|
| 品种 | OKX BTC-USDT-SWAP，成交价 K 线；若已有指定真实文件，则保留文件的真实口径，不静默切换市场 |
| 历史粒度 | 15 分钟 |
| 历史范围 | 最近 14 天完整 K 线；数量与起止时点记录在文件中 |
| 未来演示 | 24 小时，每 15 分钟一个点，共 96 个未来节点，锚点另存 |
| 路径 | 冲高回落、下探回升、持续突破；具体路径只为测试展示 |
| 范围 | 内外两层示意带；标注“示意区间，未经校准”，不宣称真实覆盖概率 |
| 网格 | 至少三个演示配置，含中性/做多/做空标签；可保留初始底仓字段，不实现成交逻辑 |
| 展示窗口 | 6 / 12 / 24 小时；裁切同一份演示数据，不重新生成预测 |
| 时区 | 存储使用 UTC；界面默认浏览器时区并明确标注，可切换 UTC；不修改原始时间戳 |
| 默认视图 | 最近约 48 小时历史 + 24 小时未来，完整历史可向左查看 |

不限制正式系统以后只能有三个模式。网格方案由数据文件枚举；不要把五种正式模式的细节靠猜测补齐。

## 3. 技术选择与边界

新空目录建议采用 Vite + TypeScript + 原生 HTML/CSS + lightweight-charts。类型/数据测试可用 Vitest，浏览器交互与截图可用 Playwright。若已经有正常工作的 React/Vue 前端，不为本任务重写框架。

- 一个前端项目，一个本地静态/开发服务；只监听 127.0.0.1。
- 无后端业务服务、数据库或登录系统。
- 当前只通过一个 `loadDataset()` 入口加载本地静态 JSON；以后可以替换这个入口，不提前设计多种存储驱动。
- 页面不调用 LLM，不读取 Codex 凭证，不需要交易所交易密钥。
- 下载脚本在 Node/已有数据工具中执行，不让页面抓取第三方交易网页。
- 依赖固定版本，提交锁文件；根据实际安装版本阅读 typings 和对应官方文档。
- 若官方示例需要额外的小依赖，记录理由；不要为了“以后扩展”引入架构组件。
- 图表库按许可证保留必要的 NOTICE 和署名链接。

## 4. 最小文档与目录

已有仓库存在同类文件时就地更新，不建立第二套事实来源。空仓库可以采用：

```text
market-forecast-viewer/
  AGENTS.md
  README.md
  docs/
    CHART_MVP.md
    DATA_CONTRACT.md
    PROGRESS.md
  scripts/
    download-history.mjs
    make-demo.mjs
    validate-data.mjs
  public/data/
    history.json
    forecast.demo.json
    grids.demo.json
  src/
    main.ts
    data.ts
    chart.ts
    forecast-band.ts
    controls.ts
    styles.css
  tests/
  artifacts/                 # 截图、测试报告，通常不提交大量生成物
  package.json
  package-lock.json
  vite.config.ts
```

这是职责划分示例，不要求为了凑文件数创建空模块。

| 文件 | 内容与唯一责任 |
|---|---|
| AGENTS.md | 本轮范围、开工顺序、权限边界、验证与交接规则 |
| docs/CHART_MVP.md | 页面行为、演示默认值、非目标、验收标准 |
| docs/DATA_CONTRACT.md | 文件字段、时间单位、数据关联、演示标识、错误处理 |
| docs/PROGRESS.md | 当前阶段、提交、测试、待验收、阻塞、变更记录与唯一下一步 |
| README.md | 已实测的安装、下载、生成、启动、停止、验证和恢复说明 |

## 5. 数据约定

### history.json：真实历史

元数据至少包含：`schema_version`、`dataset_id`、`source`、`instrument`、`market_type`、`price_type`、`bar_seconds`、`downloaded_at`、起止时间、条数与内容校验值。每条行情包含开盘时点、收盘时点、OHLC、必要成交量字段及其单位、已收盘状态。

要求：

- 从官方授权数据接口或用户已有可追溯真实文件获取。
- 只使用完整 K 线，不能把未收盘柱当成固定历史。
- 按时间排序、去重，冲突重复与缺口明确报告，不凭空补真实行情。
- 价格须为有限正数，OHLC 关系正确；成交量单位不混淆。
- 保存文件采用先校验、后替换，下载失败不得覆盖已有成功快照。
- 数据下载受地区、权限、代理或网络限制时，如实说明；不关闭 TLS 校验、不伪造行情、不未经允许切换交易所。
- 对真实下载样本的仓库存储遵守数据源条款；不要未经允许公开分发行情文件。

### forecast.demo.json：演示未来

至少包含：`schema_version`、`forecast_id`、`history_dataset_id`、`demo: true`、`source_kind: "synthetic_fixture"`、`generated_at`、`anchor_time`、`anchor_price`、`horizon_seconds`、`step_seconds`、`scenarios`、`bands`。

- 锚点绑定同一历史数据集最后完整柱的收盘价与对应收盘时点。文档明确开盘标签和收盘时点的区别。
- 每条路径保存稳定 id、名称、未来时间点与价格。线性连接是视觉表示，不是假设真实市场按直线成交。
- 可以保存 `demo_weight`，但界面必须写“示例权重/非实测概率”；权重合法且总和为 1。也可暂时不展示概率。
- 隐藏某条路径不改变原始权重，也不自动归一化余下路径。
- `bands` 保存每个未来时点的上下界与 `kind: "illustrative"`；内外带嵌套正确。没有统计验证时不要称作已校准的 80%/95% 预测区间。
- 所有未来点晚于锚点。锚点可以独立用作连接参考，不混入真实 K 线。
- 相同历史快照和固定随机种子生成完全相同的演示文件。页面刷新不能随机改写路径。
- 不把事后真实行情复制进演示路径并称为事前预测。

### grids.demo.json：演示网格配置

至少包含：`schema_version`、`demo: true`、`history_dataset_id`、方案 id、中文名称、方向/模式标签、可选初始底仓标签、上下界、等差/等比标识和明确的价格层级数组。

- 明确“区间数 N”与“价格线数量 N+1”区别，避免各处计数不一致。
- 网格线来源于配置文件而非页面硬编码。
- 上下界、每级价格合法，层级有序；不因为选择不同路径就修改网格。
- 同一方案下切换路径可见性不改变其参数。
- 本轮不保存假的净收益、胜率、推荐等级；收益区域如需预留，写“尚未接入评估”。

### 文件间校验

历史品种、演示锚点、网格基准、粒度、时间范围和数据集 id 要匹配。遇到不兼容版本、坏 JSON、缺文件或 id 冲突，停止绘制受影响的图层，给出中文原因和修复办法。不能忽略错误继续展示似乎正常的预测。

## 6. 页面规格

- 顶部紧凑显示品种、周期、历史截止时间、演示锚点、时区和常驻“演示预测｜非交易信号”。
- 主图占主要空间，桌面初始视图就能看到历史和完整未来，不能默认把未来挤出右侧。
- 历史为真实 K 线；未来用至少三条可单独开关的折线。不要用仿真 K 线伪装尚未发生的交易。
- 历史/预测分界线清楚，图上直接标注“演示未来”。
- 内外示意区间透明绘制，价格线与网格线仍可辨认。
- 图例使用文字/线型辅助，不只依赖颜色区别。
- 路径显隐与网格方案是两个独立控件。
- 网格选择后，上下界、价格层级和参数摘要同步更新，旧图层被移除。
- 支持 6/12/24 小时窗口切换、十字线提示、缩放、平移、重置视图、手动重载本地文件。
- 悬停历史显示真实 OHLC；悬停未来显示相应路径值和示意上下界，不伪造 OHLC。
- 本轮“手动重载”只重新读文件，不触发预测或联网下载。
- 真实数据源信息、许可证署名入口和数据错误区域可找到。

## 7. 图表技术重点

Codex 应参考实际安装版本的类型定义、官方 docs 与 repo 中的开发指引。

- 当前官方 v5 示例使用 `chart.addSeries(CandlestickSeries, ...)` 等方式，不混用旧版方法名。
- 内部 UTC 时间按约定存储；进入图表前统一转换到库所需秒单位，不能把毫秒直接传入。
- 每条 series 时间严格递增且不重复。
- 使用独立 LineSeries 绘制路径。历史 candle 数据保持真实。
- 使用官方公开 primitive/custom drawing 接口或适配官方 bands 示例绘制区间；不要拿历史布林带冒充预测区间。
- 使用统一价格坐标系，必要时将区间上下界纳入 autoscale；不能给预测曲线不同的隐藏价格尺度而视觉失真。
- 主图时间轴必须包含未来节点。可以用独立时间支撑 series/whitespace data；隐藏所有路径后，未来区间和网格仍须保持正确时间位置。
- 缩放、平移、窗口 resize、DPR 变化与方案切换后都应正确重绘。
- 不用固定 CSS 像素盖一个“预测阴影”糊弄，因为它不会可靠跟随图表。
- 公共 `priceToCoordinate`、`timeToCoordinate` 等 API 优先，不依赖私有内部对象。
- 清理事件监听和旧绘图对象，避免方案切换后重复图层或内存累积。

## 8. 六项任务与交付关卡

这里 C0—C5 是本任务书的编号，不是已经创建的 GitHub Issue 编号。

| 编号 | 当前要做什么 | 完成证据 | 停止点 |
|---|---|---|---|
| C0 | 仓库/环境检查、最小文档、数据约定、计划 | 当前目录与 git 状态、版本记录、文档草案、分任务列表 | 用户确认没有跑偏后再写业务代码 |
| C1 | 下载真实行情、生成固定演示文件、数据校验 | 真实来源与起止、条数、校验报告、三份可读文件 | 不接图表前先确保数据有效 |
| C2 | 最小页面与真实 K 线，建立未来时间范围 | 浏览器截图、真实数据提示、能缩放和查看历史 | 可见结果，不以纯日志代替 |
| C3 | 三条路径、区间带、分界和图例 | 初始、缩放、resize、隐藏路径后的截图/测试 | 第一处主要视觉验收点 |
| C4 | 网格方案切换、独立控件、窗口切换、异常状态 | 三套方案截图与切换测试，不含收益假数 | 只做本轮交互 |
| C5 | 独立审查、测试、安装/启动说明、最终交付 | 测试日志、构建、截图、未完成项、Git 提交 | 标为待用户验收；不得自动开始真实预测 |

建议一个实施会话顺序推进，代码审查用另一个 Codex 会话只读进行；不要在同一目录并行写同一批文件。

## 9. 必须实现并实测的命令

以下是对项目交付的要求，不表示当前已经存在：

```text
npm run data:download      # 一次性获取真实历史，不生成演示
npm run demo:generate      # 基于固定历史生成可复现演示
npm run data:validate      # 校验文件和关联
npm run dev               # 本地开发预览，绑定 127.0.0.1
npm run typecheck
npm test
npm run test:e2e
npm run build
npm run preview           # 本地查看构建产物，非公网部署
```

可以增加 `start.command` 为 Mac 提供双击启动。脚本不自动安装全局工具、不偷偷联网更新依赖；缺依赖时给出清楚提示。README 写明停止方式和端口冲突处理。

## 10. 测试与验收

### 自动验证

- 时间转换、排序去重、OHLC、未来锚点、区间上下界与嵌套、文件关联。
- 示例权重合法；图层开关不修改源数据。
- 未收盘柱被排除；真实数据缺口不伪造。
- 网格价格线数量、边界、方案切换清理。
- 坏 JSON/缺文件/不同品种/错误数据集 id 的明确错误状态。
- 桌面两种视口如 1440×900、1280×800 能打开页面，控制台无错误。
- 缩放、平移、resize、隐藏所有路径后区间与网格仍正确。
- 至少连续切换 20 次，无叠线、重复图表或监听器不断累积。
- 已下载并启动后断网，仍能查看本地静态数据，不尝试外部 CDN。
- 测试使用固定快照和种子，不在每次测试时拉最新行情。
- Canvas 绘图既检查输入数值与对象生命周期，也保存真实浏览器截图人工复核；只检查 DOM 文本不算图表验收。

截图应记录视口、时区、数据集 id、构建/提交版本。首次截图须人工认可后才作为后续回归基线；跨 OS/字体差异不能误称代码失败，也不能自动接受所有新截图。

### 用户验收的五个问题

1. 不看说明是否能分清真实历史与演示未来？
2. 是否能看出不同过程，而不仅是三小时/一天末的涨跌？
3. 切换网格后能否直观看到区间与层级变化？
4. 缩放/拖动后线与区间是否一直对齐？
5. 是否可以一次操作打开、刷新，且始终知道数据截止时间？

## 11. GitHub 与上下文规则

- 默认一个里程碑 Chart MVP，任务 C0—C5；无需增加 GitHub Project 字段体系。
- 每项小任务单独可审查提交。功能分支建议 `feat/chart-mvp`，不要自动合并。
- 已有 GitHub 远程且权限允许时，把任务、测试证据与 PR 对应起来；没有远程时用本地 Git 和 PROGRESS，不以此阻塞出图。
- 未获授权不创建远程仓库、不推送、不改变仓库可见性、不删除用户文件、不执行破坏性 reset。
- 不提交 `node_modules`、凭证或大量原始数据；固定测试快照的提交范围先检查数据授权与仓库可见性。
- 每次开工读 AGENTS、MVP、数据约定、PROGRESS 和当前任务。总结当前范围和唯一下一步。
- 每次结束记录真实命令、结果、未测试内容、截图位置、当前提交及下一步。
- 文档不是权限系统；通过小提交、测试、审核和禁止自动合并共同控制风险。

---

# 提示词 A：第一次发给 Codex（只做 C0）

你正在我的 Mac 本地项目目录中开发“市场天气预报图表 MVP”。请先完整阅读这份任务书，然后只执行 C0，不要一次开发完整项目。

已确认：使用官方 lightweight-charts 作为正式图表基础；本地浏览器显示；真实历史行情与明确标注 DEMO 的未来路径分离。要展示多条路径、分阶段示意区间、网格线和方案切换，先验收图表，再另行接入自动更新。只使用官方 Codex，不引入其他大模型或付费大模型 API；当前演示数据不是实际预报。

请先检查当前目录、Git 状态、已有 AGENTS/规格文件、Node/npm 与可用浏览器测试工具。已有文件和未提交修改不得覆盖或删除；不修改全局代理、安全设置或其他项目。

空目录推荐 Vite+TypeScript+原生页面+lightweight-charts；已有成熟前端则最小复用。不引入数据库、Docker、Next.js、复杂状态管理、后端业务服务、交易 API、定时任务或多 Agent 平台。

阅读本机安装版本的 typings、官方文档和 lightweight-charts 官方仓库中的编码助手指引；不混用旧 API。先规划两个开发难点：未来时间轴，以及可随缩放平移正确重绘的区间带。

创建或更新 AGENTS.md、docs/CHART_MVP.md、docs/DATA_CONTRACT.md、docs/PROGRESS.md 与 README.md。没有实现的启动命令只能标记为计划，不声称已验证。现有同类文件就地复用，不创建冲突副本。

任务拆为 C0 约定、C1 数据、C2 真实K线、C3 路径与区间、C4 网格交互、C5 审查交付。说明每项输入、输出、验收和不做什么。缺少远程仓库不阻塞本地工作；不要擅自创建或推送远程。

C0 完成后向我说明当前环境、建议技术栈、数据约定、拟修改文件、阶段计划与真正的阻塞项，并停止等待我批准 C1。不必研究整个旧量化项目。

# 提示词 B：数据与第一张图（C1—C2）

我批准当前图表 MVP 约定。请执行 C1 和 C2，不做后续真实预测或交易。

先获取可追溯真实历史行情。按规格记录来源、品种、周期、单位、下载时间、起止时间和条数；剔除未收盘柱；检查排序、重复、缺口、OHLC。失败就如实报告，不伪造、不静默更换市场。

基于固定历史快照生成 forecast.demo.json 和 grids.demo.json，绑定同一 dataset_id。采用固定种子、清楚的 DEMO 标识和独立元数据，刷新页面不改变演示路径。初期未来数据可先作为校验输入，不抢做区间带。

建立能读取本地文件的页面，先显示真实 K 线、来源、截止时间与演示状态。运行在 127.0.0.1，图表资源本地打包。保留未来时间区间，初始能看到近期历史而不把未来挤出视口。

运行数据校验、类型检查、相关测试并在真实浏览器中打开；提交截图和可用启动方式。更新 PROGRESS 后停在 C2。

# 提示词 C：多路径与区间（C3）

请只执行 C3。使用现有固定文件绘制至少三条不同走势的演示路径，以及内外两层示意区间和清楚的历史/未来分界。

不要只画平均线；不要把区间写成已经校准的概率；不要把示例权重当真实概率。路径显隐只影响显示，不更改权重和数据。锚点与历史收盘时点/价格按数据约定对齐。

区间采用官方支持的 primitive/公开绘图接口，基于数据坐标绘制，不用固定像素 CSS 蒙版，不把历史布林带当预测。保证同一价格坐标系、正确 autoscale 与未来时间范围。隐藏所有路径后，未来时间轴与区间仍有效。

验证初始视图、水平缩放、平移、垂直价格缩放、窗口大小变化和路径开关；查看真实浏览器截图，不只测试文本。给出可运行页面、截图、已知缺陷、测试结果，更新 PROGRESS，并停在第一轮视觉验收。

# 提示词 D：网格方案交互（C4）

主图方向已确认，请只执行 C4。

从 grids.demo.json 枚举方案，用独立控件切换；中性/做多/做空只是演示标签，不限定最终策略模式数量。不开发撮合/盈亏引擎。显示明确的层级价格、上下界、区间数与价格线数，旧方案线条必须清理。

路径开关与网格选择互不改写。新增 6/12/24 小时显示窗口、重置视图和手动重新加载文件。窗口切换只裁切同一份数据，不重新生成路径。

缺文件、坏 JSON、数据集不匹配时用中文解释，停止错误图层；不能显示假收益、假胜率或推荐交易。验证连续切换、缩放、窗口调整、截图与离线查看，更新 PROGRESS 后停止。

# 提示词 E：独立只读审查

请作为同一项目的审查者，只读检查本轮实现，不修改代码，不扩展范围。

先读任务书、AGENTS、当前规格、数据约定、PROGRESS、Git diff 和测试结果。核验是否确实使用真实行情、是否 DEMO 始终显著、锚点/时间单位正确、区间跟随坐标、历史与未来不混合、方案切换不留旧图层、没有凭空增加收益或模型能力。

检查库的实际版本和 API、使用公开接口、图表实例/监听器生命周期、未来节点、autoscale、错误状态、资源是否本地、服务绑定地址和许可证署名。

独立运行可用的数据测试、类型检查、浏览器测试和构建。不能运行的项目写未验证，不编造通过。给出问题严重程度、文件位置、复现方式和最小修复建议；不要执行修复或提出大重构。

# 提示词 F：最终交付（C5）

请在原开发会话完成 C5：只修复审查发现且属于本轮范围的问题，重跑受影响的测试，不趁机新增功能。

交付：本地可打开页面；真实行情与演示文件；一次性下载、生成、校验、测试、构建命令；Mac 简单启动与停止说明；默认/缩放/区间/至少三套网格/错误状态截图；所有实际测试结果；明确未完成和未验证项。

确保 README 从干净依赖环境可执行，PROGRESS 对应当前提交。截图包含演示标识和时间口径。图表达到自动测试要求后只标记“待用户视觉验收”，不能自行宣布预测有效。

停止，不创建真实预测、自动更新、交易接口或定时任务。

# 提示词 G：新会话恢复工作

请不要依赖之前聊天记忆。先读取本仓库 AGENTS.md、MVP 规格、数据约定、PROGRESS、当前 Git 状态和任务记录。

用六行复述：当前目标、已完成阶段、最后验证结果、未解决问题、本次唯一任务、明确不做什么。

只完成 PROGRESS 中已获批准的下一项。发现冲突时保留现有文件，指出冲突而不自行重新设计项目。完成后更新进度和证据。

---

## 官方资料索引（给 Codex 核验，不代表已经安装/执行）

- 官方 Codex/桌面入口：https://developers.openai.com/codex/app/
- Codex 账号登录：https://developers.openai.com/codex/auth
- AGENTS.md：https://developers.openai.com/codex/guides/agents-md
- Vite：https://vite.dev/guide/
- Lightweight Charts：https://github.com/tradingview/lightweight-charts
- 官方仓库编码指引：https://github.com/tradingview/lightweight-charts/blob/master/.github/skills/lightweight-charts/SKILL.md
- 库文档：https://tradingview.github.io/lightweight-charts/docs
- 插件示例：https://tradingview.github.io/lightweight-charts/plugin-examples/
- Series primitives：https://tradingview.github.io/lightweight-charts/docs/plugins/series-primitives
- WhitespaceData：https://tradingview.github.io/lightweight-charts/docs/api/interfaces/WhitespaceData
- OKX 官方历史行情文档：https://www.okx.com/docs-v5/en/#order-book-trading-market-data-get-candlesticks-history
- 官方文档镜像区域入口（阅读）：https://my.okx.com/docs-v5/zh/
- Playwright 视觉比较：https://playwright.dev/docs/test-snapshots

文档链接及接口在开发时须复核，安装版本的类型定义优先。官方产品界面可能更新；使用用户已有的官方 Codex 本地代码入口，不据旧截图猜菜单。
